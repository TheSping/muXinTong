# 牧信通 — 项目总结文档

> **项目地址**：`e:\123\muXinTong\`  
> **部署状态**：已部署至 Linux 服务器，前后端 + MySQL + 区块链运行中  
> **文档日期**：2026-07-24

---

## 一、项目概述

牧信通是一套基于 **FISCO BCOS 区块链** 的畜牧资产管理与金融融资演示平台。覆盖牛/羊（活体牲畜）的资产确权、链上存证、活体估值、防伪核验、融资预审及申请全流程。前端三端（业务B端 / 审计A端 / 金融F端）+ 后端 Java / MySQL / 区块链全部部署在同一 Linux 服务器上。

---

## 二、目录结构

```
muXinTong/
├── pom.xml                                # 后端 Maven 构建文件
├── mvnw / mvnw.cmd                        # Maven Wrapper（无需安装 Maven）
├── src/main/java/com/example/muye/
│   ├── MuyeApplication.java               # Spring Boot 启动类
│   ├── DeployContract.java                # 合约一次性部署辅助类
│   ├── common/
│   │   ├── Result.java                    # 统一响应体 {code, message, data}
│   │   └── GlobalExceptionHandler.java    # 全局异常拦截
│   ├── config/
│   │   ├── AppConfig.java                 # @ConfigurationProperties 集中管理
│   │   ├── BlockchainConfig.java          # FISCO BCOS 客户端 Bean
│   │   └── CorsConfig.java               # 全局跨域
│   ├── contract/
│   │   └── CowAssetChain.java             # Solidity 合约 Java Wrapper（牛羊共用）
│   ├── controller/
│   │   ├── CowAssetController.java        # /api/cow 牛只资产
│   │   ├── SheepAssetController.java      # /api/sheep 羊只资产
│   │   ├── SysUserController.java         # /api/user 注册/登录
│   │   └── FinanceController.java         # /api/finance 融资
│   ├── dto/
│   │   ├── ValuationRequest.java          # 估值请求
│   │   ├── FinancePredictDTO.java         # 融资预审请求
│   │   ├── FinanceSubmitDTO.java          # 融资提交请求
│   │   └── UserAuthDTO.java               # 注册/登录请求
│   ├── entity/
│   │   ├── CowAsset.java                  # cow_asset 表
│   │   ├── SheepAsset.java                # sheep_asset 表
│   │   ├── SysUser.java                   # sys_user 表
│   │   └── FinanceApplication.java        # sys_finance 表
│   ├── exception/
│   │   ├── BusinessException.java         # 业务异常基类
│   │   ├── AuthException.java             # 认证异常 (→ 401)
│   │   ├── AssetNotFoundException.java    # 资产不存在 (→ 404)
│   │   └── BlockchainException.java       # 区块链异常 (→ 502)
│   ├── mapper/
│   │   ├── CowAssetMapper.java
│   │   ├── SheepAssetMapper.java
│   │   ├── SysUserMapper.java
│   │   └── FinanceApplicationMapper.java
│   └── service/
│       ├── CowAssetService.java           # 牛核心业务
│       ├── SheepAssetService.java         # 羊核心业务
│       ├── SysUserService.java            # 用户认证
│       └── FinanceApplicationService.java # 融资管理
├── src/main/resources/
│   ├── application.properties             # 数据库、区块链、合约配置
│   └── conf/                              # FISCO BCOS SDK 证书
│       ├── ca.crt
│       ├── sdk.crt
│       └── sdk.key
├── muxintong-frontend/                    # Vue 3 前端项目
│   ├── src/api/                           # API 层（请求封装）
│   │   ├── request.js                     # Axios 实例 + 拦截器
│   │   ├── auth.js                        # 注册/登录
│   │   ├── asset.js                       # 牛资产接口
│   │   ├── sheep.js                       # 羊资产接口
│   │   ├── valuation.js                   # 估值接口
│   │   ├── finance.js                     # 融资接口
│   │   └── dashboard.js                   # 仪表盘数据
│   ├── src/router/index.js                # 路由（三端 UI）
│   ├── src/views/                         # 页面组件
│   ├── src/components/                    # 通用组件
│   ├── src/stores/                        # Pinia 状态管理
│   ├── dist/                              # 前端构建产物（部署到 nginx）
│   ├── vite.config.js                     # Vite 构建配置
│   ├── .env.development                   # 开发环境 API 地址
│   └── .env.production                    # 生产环境留空
├── 部署指南.md                             # 完整部署流程
├── PROJECT_SUMMARY.md                     # 本文件
└── muxintong-frontend/API对照报告.md      # 前后端接口对照
```

---

## 三、技术栈

| 层次 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Spring Boot | 2.7.18 |
| Java | JDK | 11 |
| ORM | MyBatis-Plus | 3.5.3.1 |
| 数据库 | MySQL | 8.x |
| 区块链 | FISCO BCOS Java SDK | 2.10.0（链版本 2.11.0） |
| 前端框架 | Vue 3 + Vite | 8.x |
| 状态管理 | Pinia | 3.x |
| 路由 | Vue Router | 4.x |
| HTTP 客户端 | Axios | 1.15 |
| 图表 | ECharts | 6.x |
| 构建工具 | Maven（后端）+ npm（前端） | — |

---

## 四、API 接口总览

所有接口统一返回 `{"code": 200, "message": "操作成功", "data": ...}`。

### 4.1 用户认证 — `/api/user`

| 方法 | 路径 | 说明 | 请求体 |
|------|------|------|--------|
| POST | `/api/user/register` | 注册 | `{username, password, phone?}` |
| POST | `/api/user/login` | 登录 | `{username, password}` → `{data.token}` |

### 4.2 牛只资产 — `/api/cow`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/cow/add` | 新增牛只（MySQL + 链） |
| POST | `/api/cow/confirm` | 确权流水线（双写 + 状态报告） |
| POST | `/api/cow/evaluate` | 活体估值（weight × 1.5 + 风险分级） |
| GET | `/api/cow/list` | 列表 |
| GET | `/api/cow/{id}` | 按 ID 查 |
| GET | `/api/cow/verify/{id}` | 防伪核验（MySQL vs 链指纹） |
| PUT | `/api/cow/update` | 修改（体重变动自动重估） |
| DELETE | `/api/cow/delete/{id}` | 删除 |

### 4.3 羊只资产 — `/api/sheep`

与牛完全相同（8 个接口），路径前缀 `/api/sheep`。

### 4.4 融资管理 — `/api/finance`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/finance/predict` | 资格预审（抵押率 60-85%，LPR 3.45% + 风险利率） |
| POST | `/api/finance/submit` | 提交申请（落库 sys_finance） |
| GET | `/api/finance/list` | 列表 |
| GET | `/api/finance/{id}` | 按 ID 查 |
| PUT | `/api/finance/update` | 修改（更新状态） |
| DELETE | `/api/finance/delete/{id}` | 删除 |

---

## 五、数据库表结构

### 5.1 `sys_user` — 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| username | VARCHAR(50) | 唯一 |
| password | VARCHAR(100) | MD5 + 盐加密 |
| phone | VARCHAR(20) | — |
| role | VARCHAR(20) | 默认 USER |
| create_time | DATETIME | — |

### 5.2 `cow_asset` — 牛资产表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键（雪花 ID） |
| ear_tag | VARCHAR(100) | 耳标号（链上 key） |
| weight | DOUBLE | 体重（kg） |
| valuation | DOUBLE | 估值（weight × 1.5） |

### 5.3 `sheep_asset` — 羊资产表

结构同 `cow_asset`。

### 5.4 `sys_finance` — 融资申请表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| applicant | VARCHAR(100) | 申请人 |
| apply_amount | DOUBLE | 申请金额 |
| loan_period | INT | 贷款周期（月） |
| loan_usage | VARCHAR(500) | 用途 |
| asset_valuation | DOUBLE | 资产估值 |
| risk_level | VARCHAR(20) | 风险等级 |
| suggested_quota | DOUBLE | 建议额度 |
| suggested_rate | DOUBLE | 建议利率 |
| status | VARCHAR(50) | 状态 |
| create_time | DATETIME | 创建时间 |

---

## 六、区块链

| 项 | 值 |
|----|-----|
| 链 | FISCO BCOS 2.11.0（单节点） |
| 节点地址 | 127.0.0.1:20200 |
| 群组 | 1 |
| 加密 | ECDSA（非国密） |
| 合约 | `CowAssetChain`（牛羊共用 key=earTag） |
| 上链哈希 | SHA-256（真实指纹） |
| 密钥 | `@PostConstruct` 初始化一次，全生命周期复用 |

---

## 七、配置一览（application.properties）

| 配置项 | 当前值 | 说明 |
|--------|--------|------|
| `server.port` | 8080 | 后端端口 |
| `spring.datasource.url` | `jdbc:mysql://127.0.0.1:3306/muxintong` | 数据库连接 |
| `spring.datasource.username` | root | — |
| `spring.datasource.password` | 123456 | — |
| `app.blockchain.peers` | 127.0.0.1:20200 | 链节点 |
| `app.blockchain.group-id` | 1 | 群组 |
| `app.blockchain.cert-path` | conf | 证书路径（classpath） |
| `app.contract.cow-asset-address` | `0xba2e6a...` | 合约部署地址 |

---

## 八、部署拓扑

```
Linux 服务器
├── FISCO BCOS 节点          (127.0.0.1:20200)
├── MySQL                     (127.0.0.1:3306, db:muxintong)
├── Java 后端                 (127.0.0.1:8080) → Muye-0.0.1-SNAPSHOT.jar
└── Nginx
    ├── 静态前端文件           (/var/www/html ← dist/)
    └── /api/ → proxy_pass    (→ 127.0.0.1:8080)
```

用户浏览器 → `http://服务器IP` → Nginx → 前端页面 + API 转发

---

## 九、架构特性

- **统一返回**：所有 API 返回 `Result<T>`（code/message/data）
- **全局异常拦截**：6 条异常映射链（Valid → 400 / Auth → 401 / NotFound → 404 / Blockchain → 502 / Business → 400 / 兜底 → 500）
- **参数校验**：`@Valid` + `javax.validation` 注解，校验失败自动 400
- **配置外置**：`@ConfigurationProperties` 绑定 `application.properties`，上链地址、合约地址均可改配置无需改代码
- **真实哈希**：SHA-256 计算 64 位十六进制数字指纹上链
- **奶密钥复用**：`CryptoKeyPair` 启动时初始化一次
- **Controller 纯粹**：所有业务逻辑下沉到 Service 层

---

## 十、演示流程

1. 浏览器打开 `http://服务器IP`
2. 门户选择 **B端** → 用演示账户 `biz_admin / 123456` 登录
3. **资产确权**：输入耳标号 + 体重 → 点击提交确权 → 弹出链上哈希和四维状态
4. **活体估值**：输入体重 + 健康评分 → 点击开始估值 → 显示估值金额 + 风险等级
5. **一键融资**：填写申请人/金额/周期/用途 → 资格预审（点一次）→ 提交融资申请（点一次）
6. 仪表盘自动读取后端牛/羊列表，显示真实牲畜数量和总估值

---

> 文档最后更新：2026-07-24
