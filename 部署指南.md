# 牧信通 — 部署指南

> **部署模式**：Windows 本地开发打包 → 上传 jar 到 Linux 服务器 → 服务器端部署链/合约/MySQL → 启动运行。
> 所有服务（MySQL、后端、前端、区块链）均在同一台 Linux 服务器上。

---

## 一、总体流程

```
Windows (开发机)                       Linux (服务器)
─────────────────                      ────────────────
1. mvn clean package                  4. 安装 JDK 11 + MySQL
2. 得到 target/Muye-*.jar              5. 搭建 FISCO BCOS 单节点链
3. scp jar → 服务器                    6. 部署智能合约 → 拿到合约地址
                                       7. 导入建表 SQL
                                       8. 改 application.properties
                                       9. java -jar 启动
```

---

## 二、Windows 端操作

### 2.1 确认 application.properties

打包前先确认配置文件中数据库和区块链的连接信息指向 Linux 服务器：

```properties
# 数据库（root / 123456，IP 为你的 Linux 服务器地址）
spring.datasource.url=jdbc:mysql://<Linux服务器IP>:3306/muxintong?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456

# 区块链节点（单节点，IP 为你的 Linux 服务器地址）
app.blockchain.peers=<Linux服务器IP>:20200
app.blockchain.group-id=1

# 合约地址（等 Linux 上部署完合约后再改这里，然后重新打包！）
app.contract.cow-asset-address=0x待部署后替换
```

> **注意**：合约地址需要等 Linux 上部署完合约后才能确定。建议先走完 Linux 端的区块链搭建和合约部署，拿到地址后回 Windows 改配置重新打包一次。

### 2.2 打包

在项目根目录执行：

```bash
./mvnw clean package -DskipTests -q
```

编译完成后 `target/` 目录下会有 `Muye-0.0.1-SNAPSHOT.jar`。

### 2.3 上传到服务器

```bash
scp target/Muye-0.0.1-SNAPSHOT.jar root@<Linux服务器IP>:/opt/
```

---

## 三、Linux 服务器端操作

以下所有命令在 Linux 服务器上执行（SSH 登录后），使用 root 用户。

### 3.1 安装 JDK 11

```bash
# Ubuntu / Debian
apt update && apt install -y openjdk-11-jdk

# CentOS
yum install -y java-11-openjdk-devel

# 验证
java -version
```

### 3.2 安装 MySQL 8.x 并建库建表

```bash
# Ubuntu 20.04+
apt install -y mysql-server
systemctl start mysql
systemctl enable mysql

# CentOS 7
yum install -y https://dev.mysql.com/get/mysql80-community-release-el7-3.noarch.rpm
yum install -y mysql-community-server
systemctl start mysqld
systemctl enable mysqld
```

**设置 root 密码和创建数据库：**

```sql
-- 登录 MySQL（Ubuntu 首次可用 sudo mysql）
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '123456';
FLUSH PRIVILEGES;

-- 创建数据库
CREATE DATABASE IF NOT EXISTS `muxintong`
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_general_ci;

USE muxintong;

-- 1. 系统用户表
CREATE TABLE `sys_user` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `username`    VARCHAR(50)  NOT NULL,
    `password`    VARCHAR(100) NOT NULL,
    `phone`       VARCHAR(20)  DEFAULT NULL,
    `role`        VARCHAR(20)  DEFAULT 'USER',
    `create_time` DATETIME     DEFAULT NULL,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 2. 牛资产表
CREATE TABLE `cow_asset` (
    `id`        BIGINT       NOT NULL,
    `ear_tag`   VARCHAR(100) DEFAULT NULL,
    `weight`    DOUBLE       DEFAULT NULL,
    `valuation` DOUBLE       DEFAULT NULL,
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 3. 羊资产表
CREATE TABLE `sheep_asset` (
    `id`        BIGINT       NOT NULL,
    `ear_tag`   VARCHAR(100) DEFAULT NULL,
    `weight`    DOUBLE       DEFAULT NULL,
    `valuation` DOUBLE       DEFAULT NULL,
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 4. 融资申请表
CREATE TABLE `sys_finance` (
    `id`              BIGINT       NOT NULL AUTO_INCREMENT,
    `applicant`       VARCHAR(100) DEFAULT NULL,
    `apply_amount`    DOUBLE       DEFAULT NULL,
    `loan_period`     INT          DEFAULT NULL,
    `loan_usage`      VARCHAR(500) DEFAULT NULL,
    `asset_valuation` DOUBLE       DEFAULT NULL,
    `risk_level`      VARCHAR(20)  DEFAULT NULL,
    `suggested_quota` DOUBLE       DEFAULT NULL,
    `suggested_rate`  DOUBLE       DEFAULT NULL,
    `status`          VARCHAR(50)  DEFAULT NULL,
    `create_time`     DATETIME     DEFAULT NULL,
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 三、FISCO BCOS 单节点链搭建

> 演示用单节点即可，一条命令完成，出块即时无需等待共识。

```bash
cd /opt

# 下载一键建链脚本
curl -#LO https://github.com/FISCO-BCOS/FISCO-BCOS/releases/download/v2.11.0/build_chain.sh
chmod +x build_chain.sh

# 搭建单节点链
./build_chain.sh -l 127.0.0.1:1 -p 30300,20200,8545 -e /opt/fisco

# 启动节点
bash /opt/fisco/127.0.0.1/start_all.sh

# 验证
ps aux | grep fisco-bcos
```

### 3.4 部署智能合约

将节点 SDK 证书复制到 `src/main/resources/conf/` 并重新打包后，用 jar 中的 `DeployContract` 部署：

```bash
cd /opt

# 把证书放到正确位置
mkdir -p work/BOOT-INF/classes/conf
cp /opt/fisco/127.0.0.1/sdk/* work/BOOT-INF/classes/conf/

# 解压 jar 的 lib
jar xf Muye-0.0.1-SNAPSHOT.jar BOOT-INF/lib/ BOOT-INF/classes/

# 运行部署类
java -cp "BOOT-INF/classes:BOOT-INF/lib/*" com.example.muye.DeployContract
```

输出示例：
```
===== 部署成功 =====
合约地址: 0xabc123...
将上面这行填到 application.properties:
app.contract.cow-asset-address=0xabc123...
```

复制合约地址。

### 3.5 回 Windows 更新配置并重新打包

拿到合约地址后，回 **Windows** 修改 `src/main/resources/application.properties`：

```properties
spring.application.name=Muye

# 数据库（IP 为 Linux 服务器地址）
spring.datasource.url=jdbc:mysql://<Linux服务器IP>:3306/muxintong?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# 区块链节点（IP 为 Linux 服务器地址）
app.blockchain.peers=<Linux服务器IP>:20200
app.blockchain.group-id=1
app.blockchain.cert-path=conf
app.blockchain.use-sm-crypto=false

# 合约地址（刚才拿到的）
app.contract.cow-asset-address=0x你的合约地址
```

重新打包并上传：

```bash
# Windows
./mvnw clean package -DskipTests -q
scp target/Muye-0.0.1-SNAPSHOT.jar root@<Linux服务器IP>:/opt/
```

### 3.6 替换证书并启动

在 **Linux** 上，把服务器本地节点的 SDK 证书塞进 jar，然后启动：

```bash
cd /opt

# 把证书塞进 jar
mkdir work && cd work
jar xf ../Muye-0.0.1-SNAPSHOT.jar BOOT-INF/classes/conf/
cp /opt/fisco/127.0.0.1/sdk/* BOOT-INF/classes/conf/
jar uf ../Muye-0.0.1-SNAPSHOT.jar -C BOOT-INF/classes conf/
cd /opt && rm -rf work

# 后台启动
nohup java -jar Muye-0.0.1-SNAPSHOT.jar > app.log 2>&1 &
```

### 3.7 验证

```bash
curl http://localhost:8080/api/cow/list
# 返回: {"code":200,"message":"操作成功","data":[]}
```

---

## 四、一键启动脚本

服务器上把以下内容保存为 `/opt/start.sh`：

```bash
#!/bin/bash
# 启动 FISCO BCOS 节点
bash /opt/fisco/127.0.0.1/start_all.sh

# 启动后端
cd /opt
nohup java -jar Muye-0.0.1-SNAPSHOT.jar > app.log 2>&1 &
echo "后端已启动，PID=$!"

# 查看日志
tail -f app.log
```

---

## 五、单节点链的注意事项

| 项 | 说明 |
|----|------|
| 节点数 | 1 个即可，演示完全够用 |
| 共识 | 单节点自动出块，无需等待共识 |
| 端口 | P2P 30300 / Channel 20200 / RPC 8545 |
| 证书 | 节点 `sdk/` 目录下三个文件，必须与 jar 内的一致 |
| 合约 | 牛羊共用同一个合约地址，以 earTag 为 key 区分 |

---

## 六、故障排查

| 现象 | 原因 | 解决 |
|------|------|------|
| 启动报 `Communications link failure` | MySQL 没启动或 root 密码不是 123456 | `systemctl start mysql`，检查密码 |
| 确权返回 502 | 区块链节点没启动 | `bash /opt/fisco/127.0.0.1/start_all.sh` |
| 确权返回 502 + cert 相关报错 | 证书不匹配 | 重新把节点 sdk/ 证书塞进 jar |
| 核验返回空哈希 | 合约没部署或地址配错 | 检查 `application.properties` 中的合约地址 |

---

> 文档最后更新：2026-07-24
